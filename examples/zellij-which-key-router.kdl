// Repo-local integration example for ~/.config/zellij/config.kdl.
// Build first: ./scripts/build-plugin
// Local plugin URL: file:/Users/aliakseizakharau/proj/zwk/target/zellij/zellij-which-key-router.wasm

keybinds clear-defaults=true {
    locked {
        bind "Ctrl Space" {
            LaunchOrFocusPlugin "file:/Users/aliakseizakharau/proj/zwk/target/zellij/zellij-which-key-router.wasm" {
                floating true
                borderless true
                move_to_focused_tab true
            };
            SwitchToMode "Normal";
            FocusPreviousPane;
        }

        // Rollback if the overlay fails:
        // bind "Ctrl Space" {
        //     SwitchToMode "Normal";
        // }
    }

    normal {
        bind "Ctrl Space" "Esc" { SwitchToMode "Locked"; }
    }
}

plugins {
    which-key-router location="file:/Users/aliakseizakharau/proj/zwk/target/zellij/zellij-which-key-router.wasm"

    // Keep status/hint plugins independent of this floating overlay.
}

// The overlay is passive: it reads Zellij's current keymap and renders hints.
// It does not intercept keys and does not run actions itself.
//
// Requested permissions:
// - ReadApplicationState
// - ChangeApplicationState
//
// macOS fallback if the interactive prompt is unreliable: pre-approve the same
// plugin URL in Zellij's permissions.kdl using the normal permission-cache shape:
// "file:/Users/aliakseizakharau/proj/zwk/target/zellij/zellij-which-key-router.wasm" {
//     ReadApplicationState
//     ChangeApplicationState
// }
